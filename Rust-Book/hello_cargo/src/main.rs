fn main() {
    println!("Hello, cargo!");
}

// println!() is not a function, if it is a function it won't have a "!".
// only when we are calling macros, we use '!'

// > cargo build   /* only build */
// > cargo run     /* builds & execute code */